{% comment %}
  AYOVA MAIN SHOP / COLLECTION SECTION WITH FILTERS & PAGINATION
  Figma Spec: Category Banner, Sidebar Filters (Wellness Goals, Price, Stock), Sort, Grid, and Pagination
{% endcomment %}

<div class="section-padding collection-page">
  <div class="container">
    
    <!-- Collection Banner Header -->
    <div class="collection-banner">
      <div class="banner-content">
        <span class="badge badge-category">AYUSH & ISO CERTIFIED FORMULATIONS</span>
        <h1 class="collection-title">{{ collection.title | default: "All Ayurvedic Products" }}</h1>
        <p class="collection-desc">{{ collection.description | default: "Explore doctor-backed Ayurvedic supplements formulated for daily wellness, liver care, PCOS management, and immunity." }}</p>
      </div>
    </div>

    <!-- Collection Layout: Filter Sidebar + Product Grid -->
    <div class="collection-layout">
      
      <!-- Sidebar Filters -->
      <aside class="collection-filters">
        <form class="filter-form" id="CollectionFiltersForm">
          <div class="filter-header">
            <h3 class="filter-heading">Filters</h3>
            <a href="/collections/all" class="clear-filters-btn" style="text-decoration:none;">Clear All</a>
          </div>
          
          <!-- Price Filter (Moved to Top) -->
          {% for filter in collection.filters %}
            {% if filter.type == 'price_range' %}
              {% assign max_price = filter.range_max | divided_by: 100.0 | ceil %}
              {% if max_price == 0 %}{% assign max_price = 1000 %}{% endif %}
              
              {% assign current_min = 0 %}
              {% if filter.min_value.value %}
                {% assign current_min = filter.min_value.value | divided_by: 100.0 | floor %}
              {% endif %}
              
              {% assign current_max = max_price %}
              {% if filter.max_value.value %}
                {% assign current_max = filter.max_value.value | divided_by: 100.0 | ceil %}
              {% endif %}
              
              <div class="filter-group">
                <h4 class="filter-title">{{ filter.label }}</h4>
                <div class="price-slider-container">
                  <div class="price-slider-track"></div>
                  <input type="range" class="price-range-slider min-slider" min="0" max="{{ max_price }}" value="{{ current_min }}" step="1">
                  <input type="range" class="price-range-slider max-slider" min="0" max="{{ max_price }}" value="{{ current_max }}" step="1">
                </div>
                <div class="price-inputs" style="display:flex; align-items:center; gap:8px;">
                  <div style="position:relative; width:100%;">
                    <span style="position:absolute; left:8px; top:50%; transform:translateY(-50%); color:var(--color-text-muted); font-size:0.85rem;">Rs.</span>
                    <input type="number" 
                           name="{{ filter.min_value.param_name }}" 
                           id="Filter-{{ filter.label | escape }}-GTE"
                           value="{% if filter.min_value.value %}{{ current_min }}{% endif %}"
                           placeholder="0" 
                           class="filter-price-input price-input-min" 
                           min="0"
                           max="{{ max_price }}"
                           style="padding-left:28px; width:100%; border:1px solid var(--color-sage-border); border-radius:4px; height:36px;">
                  </div>
                  <span class="price-sep" style="color:var(--color-text-muted);">-</span>
                  <div style="position:relative; width:100%;">
                    <span style="position:absolute; left:8px; top:50%; transform:translateY(-50%); color:var(--color-text-muted); font-size:0.85rem;">Rs.</span>
                    <input type="number" 
                           name="{{ filter.max_value.param_name }}" 
                           id="Filter-{{ filter.label | escape }}-LTE"
                           value="{% if filter.max_value.value %}{{ current_max }}{% endif %}"
                           placeholder="{{ max_price }}" 
                           class="filter-price-input price-input-max"
                           min="0"
                           max="{{ max_price }}"
                           style="padding-left:28px; width:100%; border:1px solid var(--color-sage-border); border-radius:4px; height:36px;">
                  </div>
                </div>
              </div>
            {% endif %}
          {% endfor %}

          <!-- Categories List (Custom Fallback for Collections) -->
          <div class="filter-group">
            <div style="display:flex; justify-content:space-between; align-items:center;">
              <h4 class="filter-title">Categories</h4>
              {% if collection.handle != 'all' %}
                <a href="/collections/all" class="clear-filters-btn" style="text-decoration:none; font-size:0.75rem;">Clear</a>
              {% endif %}
            </div>

            {% for c in collections %}
              {% if c.all_products_count > 0 and c.handle != 'all' and c.handle != 'frontpage' and c.title != 'All products' %}
                <label class="filter-label" style="cursor:pointer;">
                  <input type="checkbox" class="filter-input-chk custom-cat-link" data-url="{% if collection.handle == c.handle %}/collections/all{% else %}{{ c.url }}{% endif %}" {% if collection.handle == c.handle %}checked{% endif %}>
                  {{ c.title }} ({{ c.all_products_count }})
                </label>
              {% endif %}
            {% endfor %}
          </div>

          <!-- Tags Filter (Custom Form Implementation) -->
          {% if collection.all_tags.size > 0 %}
            <div class="filter-group">
              <div style="display:flex; justify-content:space-between; align-items:center;">
                <h4 class="filter-title">Tags</h4>
                {% if current_tags %}
                  <button type="button" class="clear-filters-btn" style="font-size:0.75rem;" onclick="document.querySelectorAll('.custom-tag-checkbox').forEach(i=>i.checked=false); document.getElementById('CollectionFiltersForm').dispatchEvent(new Event('change'));">Clear</button>
                {% endif %}
              </div>
              {% for tag in collection.all_tags %}
                <label class="filter-label">
                  <input type="checkbox" class="filter-input-chk custom-tag-checkbox" value="{{ tag | handleize }}">
                  {{ tag }}
                </label>
              {% endfor %}
            </div>
          {% endif %}

          {% for filter in collection.filters %}
            {% if filter.type != 'price_range' %}
              <div class="filter-group">
                <h4 class="filter-title">{{ filter.label }}</h4>
                {% for value in filter.values %}
                  <label class="filter-label" for="Filter-{{ filter.label | escape }}-{{ forloop.index }}">
                    <input type="checkbox" 
                           class="filter-input-chk" 
                           name="{{ value.param_name }}" 
                           value="{{ value.value }}" 
                           id="Filter-{{ filter.label | escape }}-{{ forloop.index }}"
                           {% if value.active %}checked{% endif %}
                           {% if value.count == 0 and value.active == false %}disabled{% endif %}>
                    {{ value.label | escape }} ({{ value.count }})
                  </label>
                {% endfor %}
              </div>
            {% endif %}
          {% endfor %}
        </form>
      </aside>

      <!-- Main Product Grid & Controls -->
      <main class="collection-products" id="ProductGridContainer">
        {% paginate collection.products by 12 %}
        <div class="collection-toolbar">
          <span class="product-count">Showing <strong>{{ collection.products_count }}</strong> Products</span>
          <div class="sort-box">
            <label for="sort-select">Sort by:</label>
            <select id="sort-select" class="sort-select" name="sort_by" form="CollectionFiltersForm">
              {% assign sort_by = collection.sort_by | default: collection.default_sort_by %}
              {% for option in collection.sort_options %}
                <option value="{{ option.value }}" {% if option.value == sort_by %}selected{% endif %}>{{ option.name }}</option>
              {% endfor %}
            </select>
          </div>
        </div>

        <div class="grid grid-cols-3">
          {% if collection.products.size == 0 %}
            <div style="grid-column: 1 / -1; text-align: center; padding: 40px;">
              <p>No products found matching your filters.</p>
              <a href="{{ collection.url }}" style="color: var(--color-terracotta); text-decoration: underline;">Clear Filters</a>
            </div>
          {% else %}
            {% for product in collection.products %}
              {% render 'product-card', product: product %}
            {% endfor %}
          {% endif %}
        </div>

        <!-- Pagination Component -->
        {% if paginate.pages > 1 %}
          <div class="pagination-wrapper">
            <nav class="pagination">
              {% if paginate.previous %}
                <a href="{{ paginate.previous.url }}" class="page-link prev">← Prev</a>
              {% else %}
                <span class="page-link prev disabled">← Prev</span>
              {% endif %}
              
              {% for part in paginate.parts %}
                {% if part.is_link %}
                  <a href="{{ part.url }}" class="page-link">{{ part.title }}</a>
                {% else %}
                  {% if part.title == paginate.current_page %}
                    <span class="page-link active">{{ part.title }}</span>
                  {% else %}
                    <span class="page-dots">{{ part.title }}</span>
                  {% endif %}
                {% endif %}
              {% endfor %}

              {% if paginate.next %}
                <a href="{{ paginate.next.url }}" class="page-link next">Next →</a>
              {% else %}
                <span class="page-link next disabled">Next →</span>
              {% endif %}
            </nav>
          </div>
        {% endif %}
        {% endpaginate %}
      </main>

    </div>
  </div>
</div>

<style>
.collection-banner {
  background: linear-gradient(135deg, var(--color-sage-light) 0%, var(--color-cream-bg) 100%);
  padding: 44px;
  border-radius: var(--radius-lg);
  margin-bottom: 36px;
  border: 1px solid var(--color-sage-border);
}

.collection-title {
  font-family: var(--font-heading);
  font-weight: 700;
  font-size: 2.4rem;
  color: var(--color-forest-primary);
  margin: 8px 0;
}

.collection-desc {
  color: var(--color-text-muted);
  max-width: 720px;
  font-size: 1.05rem;
}

.collection-layout {
  display: grid;
  grid-template-columns: 280px 1fr;
  gap: 36px;
}

.collection-filters {
  background: var(--color-white);
  padding: 24px;
  border-radius: var(--radius-lg);
  border: 1px solid var(--color-sage-border);
  height: fit-content;
  box-shadow: var(--shadow-sm);
}

.filter-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 20px;
  padding-bottom: 12px;
  border-bottom: 1px solid var(--color-sage-border);
}

.filter-heading {
  font-family: var(--font-heading);
  font-size: 1.15rem;
  font-weight: 700;
  color: var(--color-forest-primary);
}

.clear-filters-btn {
  background: none;
  border: none;
  color: var(--color-terracotta);
  font-size: 0.8rem;
  font-weight: 600;
  cursor: pointer;
}

.filter-group {
  margin-bottom: 24px;
}

.filter-title {
  font-size: 0.9rem;
  font-weight: 700;
  margin-bottom: 12px;
  color: var(--color-text-dark);
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.filter-label {
  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 0.85rem;
  color: var(--color-text-muted);
  margin-bottom: 10px;
  cursor: pointer;
}

.filter-input-chk {
  accent-color: var(--color-forest-mid);
  width: 16px;
  height: 16px;
}

.price-inputs {
  display: flex;
  align-items: center;
  gap: 8px;
}

.filter-price-input {
  width: 100%;
  padding: 8px 12px;
  border: 1px solid var(--color-sage-border);
  border-radius: var(--radius-sm);
  font-size: 0.85rem;
}

.collection-toolbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24px;
  background: var(--color-white);
  padding: 14px 20px;
  border-radius: var(--radius-md);
  border: 1px solid var(--color-sage-border);
}

.product-count {
  font-size: 0.9rem;
  color: var(--color-text-muted);
}

.sort-box {
  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 0.85rem;
  color: var(--color-text-muted);
}

.sort-select {
  padding: 8px 14px;
  border: 1px solid var(--color-sage-border);
  border-radius: var(--radius-sm);
  background: var(--color-white);
  color: var(--color-forest-primary);
  font-weight: 600;
}

/* Pagination Styles */
.pagination-wrapper {
  margin-top: 48px;
  display: flex;
  justify-content: center;
}

.pagination {
  display: flex;
  align-items: center;
  gap: 8px;
  background: var(--color-white);
  padding: 8px 16px;
  border-radius: var(--radius-full);
  border: 1px solid var(--color-sage-border);
  box-shadow: var(--shadow-sm);
}

.page-link {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-width: 36px;
  height: 36px;
  padding: 0 12px;
  border-radius: 50%;
  font-size: 0.9rem;
  font-weight: 600;
  color: var(--color-forest-primary);
  transition: all var(--transition-fast);
}

.page-link.prev, .page-link.next {
  border-radius: var(--radius-full);
}

.page-link:hover {
  background: var(--color-sage-light);
  color: var(--color-forest-mid);
}

.page-link.active {
  background: var(--color-forest-mid);
  color: var(--color-white);
}

.page-link.disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

.page-dots {
  color: var(--color-text-muted);
  padding: 0 4px;
}

.price-slider-container {
  position: relative;
  width: 100%;
  height: 24px;
  margin-bottom: 20px;
}

.price-slider-track {
  position: absolute;
  width: 100%;
  height: 4px;
  background-color: var(--color-sage-border);
  top: 50%;
  transform: translateY(-50%);
  border-radius: 2px;
  pointer-events: none;
}

.price-range-slider {
  -webkit-appearance: none;
  appearance: none;
  width: 100%;
  position: absolute;
  pointer-events: none;
  background: transparent;
  top: 50%;
  transform: translateY(-50%);
  margin: 0;
}

.price-range-slider::-webkit-slider-thumb {
  -webkit-appearance: none;
  pointer-events: all;
  width: 16px;
  height: 16px;
  background: var(--color-forest-mid);
  border-radius: 50%;
  cursor: pointer;
  border: 2px solid var(--color-white);
  box-shadow: var(--shadow-sm);
}

.price-range-slider::-moz-range-thumb {
  pointer-events: all;
  width: 16px;
  height: 16px;
  background: var(--color-forest-mid);
  border-radius: 50%;
  cursor: pointer;
  border: 2px solid var(--color-white);
  box-shadow: var(--shadow-sm);
}

.price-range-slider.min-slider {
  z-index: 3;
}

.price-range-slider.max-slider {
  z-index: 2;
}

.price-range-slider:active {
  z-index: 4 !important;
}

@media (max-width: 900px) {
  .collection-layout {
    grid-template-columns: 1fr;
  }
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
  const filterForm = document.getElementById('CollectionFiltersForm');
  const sortSelect = document.getElementById('sort-select');

  if (!filterForm) return;

  function restoreCustomFilters() {
    // Restore Tags from URL path (Legacy Tag Filtering)
    const currentPath = window.location.pathname;
    const parts = currentPath.split('/');
    // Format is /collections/{handle}/{tags}
    if (parts.length >= 4) {
      const tagsString = parts.slice(3).join('/'); // In case there are multiple
      const activeTags = tagsString.split('+');
      
      const tagInputs = document.querySelectorAll('.custom-tag-checkbox');
      tagInputs.forEach(input => {
        if (activeTags.includes(input.value)) {
          input.checked = true;
        }
      });
    }
    
    updateSliderTrack();
  }
  
  function updateSliderTrack() {
    const parent = document.querySelector('.price-slider-container');
    if(!parent) return;
    const minSlider = parent.querySelector('.min-slider');
    const maxSlider = parent.querySelector('.max-slider');
    const track = parent.querySelector('.price-slider-track');
    
    if(minSlider && maxSlider && track) {
      const minVal = parseInt(minSlider.value) || 0;
      const maxVal = parseInt(maxSlider.value) || parseInt(maxSlider.max);
      const max = parseInt(minSlider.max) || 1000;
      
      const percentMin = (minVal / max) * 100;
      const percentMax = (maxVal / max) * 100;
      
      track.style.background = `linear-gradient(to right, var(--color-sage-border) ${percentMin}%, var(--color-forest-mid) ${percentMin}%, var(--color-forest-mid) ${percentMax}%, var(--color-sage-border) ${percentMax}%)`;
    }
  }

  function renderPage(searchParams, pathname = window.location.pathname) {
    // Add loading effect
    const gridContainer = document.getElementById('ProductGridContainer');
    if(gridContainer) {
      gridContainer.style.opacity = '0.5';
      gridContainer.style.pointerEvents = 'none';
    }

    const url = `${pathname}?${searchParams.toString()}`;

    fetch(url)
      .then(response => response.text())
      .then(responseText => {
        const html = new DOMParser().parseFromString(responseText, 'text/html');
        
        // Update product grid
        const newProductGrid = html.getElementById('ProductGridContainer');
        if (newProductGrid && gridContainer) {
          gridContainer.innerHTML = newProductGrid.innerHTML;
        }

        // Update URL without reload
        history.pushState({ path: url }, '', url);

        // Update filters sidebar to reflect new active states for categories/tags
        const newFilters = html.getElementById('CollectionFiltersForm');
        const currentFilters = document.getElementById('CollectionFiltersForm');
        if(newFilters && currentFilters) {
          currentFilters.innerHTML = newFilters.innerHTML;
          attachCategoryListeners(); // Re-attach listeners to new DOM elements
          restoreCustomFilters(); // Restore custom tags state based on new URL
        }

        // Remove loading state
        if(gridContainer) {
          gridContainer.style.opacity = '1';
          gridContainer.style.pointerEvents = 'auto';
        }
      })
      .catch(e => {
        console.error(e);
        if(gridContainer) {
          gridContainer.style.opacity = '1';
          gridContainer.style.pointerEvents = 'auto';
        }
      });
  }

  // Handle form changes (Checkboxes, Inputs, Sliders)
  filterForm.addEventListener('change', function(e) {
    // Ignore category clicks as they are handled separately
    if(e.target.classList.contains('custom-cat-link')) return;

    // Handle Dual Slider Linkage
    if (e.target.classList.contains('price-range-slider')) {
      const parent = e.target.closest('.filter-group');
      const minSlider = parent.querySelector('.min-slider');
      const maxSlider = parent.querySelector('.max-slider');
      const minInput = parent.querySelector('.price-input-min');
      const maxInput = parent.querySelector('.price-input-max');

      let minVal = parseInt(minSlider.value);
      let maxVal = parseInt(maxSlider.value);

      if (minVal >= maxVal) {
        if (e.target.classList.contains('min-slider')) {
          minVal = Math.max(0, maxVal - 1);
          minSlider.value = minVal;
        } else {
          maxVal = Math.min(parseInt(maxSlider.max), minVal + 1);
          maxSlider.value = maxVal;
        }
      }

      minInput.value = minVal;
      maxInput.value = maxVal;
      updateSliderTrack();
    }
    
    if (e.target.classList.contains('filter-price-input')) {
      const parent = e.target.closest('.filter-group');
      const minSlider = parent.querySelector('.min-slider');
      const maxSlider = parent.querySelector('.max-slider');
      const minInput = parent.querySelector('.price-input-min');
      const maxInput = parent.querySelector('.price-input-max');
      
      minSlider.value = minInput.value || 0;
      maxSlider.value = maxInput.value || maxSlider.max;
      updateSliderTrack();
    }

    const formData = new FormData(filterForm);
    const searchParams = new URLSearchParams(formData);
    
    // Manual Tag filtering URL build
    const checkedTags = Array.from(document.querySelectorAll('.custom-tag-checkbox:checked')).map(cb => cb.value);
    
    // Get base path up to the collection handle
    let basePath = window.location.pathname;
    const parts = basePath.split('/');
    if(parts.length >= 3 && parts[1] === 'collections') {
      basePath = '/' + parts[1] + '/' + parts[2];
    } else {
      basePath = '/collections/all'; // Fallback
    }
    
    if (checkedTags.length > 0) {
      basePath += '/' + checkedTags.join('+');
    }

    renderPage(searchParams, basePath);
  });

  // Handle Sort Select change
  if(sortSelect) {
    sortSelect.addEventListener('change', function(e) {
      document.getElementById('CollectionFiltersForm').dispatchEvent(new Event('change'));
    });
  }

  // Handle Category Clicks
  function attachCategoryListeners() {
    const catLinks = document.querySelectorAll('.custom-cat-link');
    catLinks.forEach(link => {
      link.addEventListener('click', function(e) {
        e.preventDefault(); // Prevent standard checkbox toggle visually until fetch completes
        const url = this.getAttribute('data-url');
        const pathname = url.split('?')[0];
        
        // Preserve other filters (price, tags, etc.)
        const formData = new FormData(document.getElementById('CollectionFiltersForm'));
        const searchParams = new URLSearchParams(formData);
        
        renderPage(searchParams, pathname);
      });
    });
  }
  
  attachCategoryListeners();
  restoreCustomFilters(); // Restore tags state on initial load
});
</script>

{% schema %}
{
  "name": "Main Collection Section",
  "settings": [],
  "presets": [
    {
      "name": "Main Collection Section"
    }
  ]
}
{% endschema %}
